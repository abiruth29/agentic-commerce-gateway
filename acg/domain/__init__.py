"""Domain core.

Value types and declarations the rule engine reasons over. Nothing in this
package performs I/O, talks to a provider, or enforces a policy — enforcement
lives in the Layer 1 engine that consumes these types.
"""
